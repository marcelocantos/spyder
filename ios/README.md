# KeepAwake

Minimal SwiftUI companion app. Single responsibility: set
`UIApplication.shared.isIdleTimerDisabled = true` on appear; unset on
background.

- **Bundle ID**: `com.marcelocantos.spyder.KeepAwake`
- **Targets**: iOS 17+ (iPhone + iPad)

Xcode project to be added under this directory.
