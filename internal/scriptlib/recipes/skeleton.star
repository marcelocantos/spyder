# Durable host Starlark skeleton (🎯T108).
# Run: spyder run-script skeleton
#   or app_exec with script_path="skeleton"
# Params dict is always injected (may be empty).

emit({"recipe": "skeleton", "ok": True, "params": params})
